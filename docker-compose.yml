version: "3.8"

services:
  sessionguard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sessionguard
    ports:
      - "8000:8000"
    environment:
      - SESSIONGUARD_DEV_MODE=true
      - SECRET_KEY=<set-a-local-secret-key>
    volumes:
      - ./storage:/app/storage
      - ./config:/app/config
    restart: unless-stopped
