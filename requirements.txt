# SessionGuard — Python Dependencies v1.2.0
# Install: pip install -r requirements.txt

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Auth (v1.2.0) ──────────────────────────────────────────────────────────────
PyJWT==2.7.0
# Password hashing via stdlib hashlib.pbkdf2_hmac — no passlib/bcrypt needed

# ── Data / Validation ─────────────────────────────────────────────────────────
pydantic==2.7.0
pandas==2.2.2

# ── Desktop Shell ─────────────────────────────────────────────────────────────
PySide6==6.7.0
# Optional browser embed:
# PySide6-WebEngineWidgets==6.7.0

# ── Video / OCR Pipeline ──────────────────────────────────────────────────────
opencv-python==4.9.0.80
pytesseract==0.3.10
Pillow==10.3.0
# easyocr==1.7.1          # GPU-accelerated OCR fallback (optional — auto-used when Tesseract confidence < 0.75)
# onnxruntime==1.18.0     # ONNX Runtime for EasyOCR GPU (optional — auto-used if installed)

# ── Behavior Engine (sklearn patterns) ────────────────────────────────────────
scikit-learn==1.5.0
numpy==1.26.4
scipy==1.13.1

# ── Export (PDF + Excel) ──────────────────────────────────────────────────────
reportlab==4.1.0
openpyxl==3.1.2

# ── AI / Intelligence (v1.2.0) ────────────────────────────────────────────────
anthropic==0.39.0           # Claude API client
# pgvector==0.2.4          # Embeddings + similarity (PostgreSQL + pgvector)

# ── HTTP client (used by backend/routes/updater.py) ───────────────────────────
requests==2.34.2

# ── Security (Phase 2: A8 upload validation) ───────────────────────────────────
pyclamd==0.4.0              # ClamAV virus scanning (optional - skip if unavailable)

# ── Observability (Phase 1: A4 structured logging) ────────────────────────────
structlog==24.1.0

# ── Environment (Phase 2: A3 .env support) ────────────────────────────────────
python-dotenv==1.0.1

# ── Future / SaaS (uncomment when needed) ────────────────────────────────────
# redis==5.0.4            # Job queue broker
# celery==5.4.0           # Distributed workers
# asyncpg==0.29.0         # PostgreSQL driver
# alembic==1.13.1         # DB migrations
# psycopg2-binary==2.9.9  # PostgreSQL adapter

# ── Optional: SQLCipher encryption (requires libsqlcipher-dev system package) ──
# pysqlcipher3

# ── Development / Testing ─────────────────────────────────────────────────────
pytest==8.2.0
httpx==0.27.0
pytest-cov==5.0.0
# pytest-asyncio==0.23.0  # Add back when async tests are needed
# ruff==0.5.0
# mypy==1.10.0